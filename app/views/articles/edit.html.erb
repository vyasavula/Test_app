<h1>Edit the existing article</h1>

<% if @article.errors.any? %>
<ul>
<% @article.errors.full_messages.each do |msg|%>
<li><%= msg %></li>
<% end %>
</ul>
<% end %>
<!Now we add a form,its a model back form and rails provides a form for helpers, find it in rails forms for helpers online>
<%= form_for @article do |f| %>
<p>
  <%= f.label :title %> <br/>
  <%=f.text_field :title %>
  <!text_field because we need something to be typed and for description as we need a larger area we take the text area fo it>
</p>
<p>
  <%= f.label :description %> <br/>
  <%= f.text_area :description %>
</p>
<p>
  <%= f.submit %>
</p>


<% end %>
<%= link_to "Back to articles listing", articles_path %>