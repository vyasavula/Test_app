<%= render 'shared/errors', obj: @category%>
<!Now we add a form,its a model back form and rails provides a form for helpers, find it in rails forms for helpers online>



      <%= form_for(@category, :html => {class: "form-horizontal", role: "form"}) do |f| %>
      <div class= "form-group">
        <div class= "control-label col-sm-2">
        <%= f.label :name %> 
          </div>
          <div class="col-sm-8">
        <%=f.text_field :name, class:"form-control", placeholder: " Category name", autofocus: true %>
        </div>
        </div>
        <!text_field because we need something tobe typed and for description as we need a larger area we take the text area fo it>
        </div>
      <div class="form-group">
       <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit class: 'btn btn-primary btn-lg' %>
 </div> 
  </div> 

      <% end %>
      <div class= "col-xs-4 col-xs-offset-4">
     [ <%= link_to "Cancel request and return to articles listing", articles_path %> ]
      </div>